import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  content: {
    border: "1px solid rgba(168,169,174,.25)",
    backgroundColor: "#FFFFFF",
    minHeight: "80vh",
  },
  padding: {
    paddingLeft: 10,
    paddingRight: 10,
    paddingTop: 10,
    paddingBottom: 10,
  },
});
